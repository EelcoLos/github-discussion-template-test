title: "[ADR] "
labels: ["ADR"]
body:
  - type: dropdown
    attributes:
      label: Status
      options:
        - Draft
        - Proposed
        - Accepted
        - Rejected
        - Deprecated
        - Superseded
      default: 0
    validations:
      required: true
  - type: markdown
    description: "What is the issue that we are seeing that is motivating this decision or change?"
    attributes:
      label: Context
  - type: markdown
    description: "What is the change that we are proposing and/or doing?"
    attributes:
      label: Decision
  - type: markdown
    description: "What becomes easier or more difficult to do because of this change?"
    attributes:
      label: Consequences
